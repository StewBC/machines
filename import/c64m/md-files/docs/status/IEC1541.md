# IEC / 1541 status

## Current implementation

- VIA 6522 module implemented and unit-tested (Phase 1 complete).
- 1541 machine shell: CPU, VIAs, memory map, ROM loading, cycle-step (Phase 2 complete).
- IEC bus wiring (Phase 3 complete):
  - `c1541_update_iec_bus()` runs every cycle: serial VIA ($1800, `via1`) Port B output bits → `c64_set_iec_external_pull()` → combined bus state → serial VIA input bits + CA1.
  - CA1/ATN IRQ: ATN assertion feeds `via6522_set_ca1()`, which sets the serial VIA IFR bit 1 on the falling edge; the 1541 CPU IRQ fires via the existing callback.
  - Serial VIA PB4 ATN acknowledge is modeled: while the C64 asserts ATN, PB4 configured output-low makes the drive pull DATA low immediately, matching the 1541 serial attention handshake.
  - New accessors: `c64_get_iec_c64_pull()` (reads CIA #2 Port A without side effects), `c64_get_drive_slot()` (returns mounted D64 slot pointer).
  - New peek helper: `cia_peek_port_a_output()` — returns raw ORA masked by DDRA without triggering port-input callbacks.
- D64 sector access via ROM intercept (Phase 3 complete):
  - Intercept at `reed` ($F4CA in DOS 2.6: 325302-01 + 901229-06AA). Job queue model: `jobn` at $3F, `hdrs[]` at $06, buffers at `$0300+N×$0100`.
  - On success: fills 1541 RAM buffer from mounted D64 image, sets PC to `read40` ($F505); on error: sets A = $02, PC to `errr` ($F969).
  - Queued READ/SEARCH jobs are satisfied from the mounted D64 image, including ROM SEARCH jobs whose queued command byte keeps its low-bit flag set.
  - Inline `d64_sector_offset()` table avoids `tools/d64/` dependency.
  - Guard: job index ≥ 5 returns early (job 5 = command channel, never a READ).
- D64 sector WRITE via ROM intercept (Phase 4 complete):
  - WRITE job (`$90`, `& $78 == $10`) handled at the same job-dispatch window and
    physical-job fallback as READ. DOS fills the job buffer before queuing the
    job, so `c1541_copy_job_buffer_to_sector()` persists `$0300+N×$0100` back into
    the mounted image at the target sector via `c64_get_drive_slot_mut()`.
  - Writable gating: WRITE to a read-only slot writes nothing and returns the
    write-protect job status ($08 → DOS 26); success sets `slot->dirty` so the
    runtime flush persists the image to the host `.d64`. No file I/O in `machine/`.
  - Covers SAVE, sequential/relative file writes, and BAM/directory updates.
- DOS command & error channel (Phase 5 complete):
  - Scratch (`S0:`), rename (`R0:`), validate (`V0`), and the error/status channel
    (`OPEN 15,8,15`) all work via the real ROM with no dedicated code — they are
    ordinary directory/BAM READ/WRITE jobs satisfied by Phase 3 + Phase 4.
  - Format (`N0:name,id`) is handled by intercepting the DOS FORMT EXECUTE job
    (`$E0`, `& $78 == $60`) in `c1541_format_track()`: the target track's sectors
    are erased in the writable image and the job returns success, after which the
    ROM's DOS writes a fresh BAM + directory (name/ID) through normal WRITE jobs.
    Read-only mounts return write-protect. No `d64_image_format()` helper needed.
- KERNAL LOAD trap coexistence:
  - `emulate_1541` flag in `c64_config` enables the 1541 ROM path for raw IEC / drive-side code.
  - Threaded through: `app_options.emulate_1541` → `runtime_config.machine_config` → `c64_config` → `c64_set_config()`.
  - INI key: `[disk] emulate_1541` (only written to INI when true).
  - When `emulate_1541` is enabled and a 1541 ROM is loaded for device 8/9, the C64 KERNAL LOAD trap is disabled for that device. Standard KERNAL `LOAD` runs through the real C64 KERNAL IEC path and the real 1541 DOS ROM serial handlers.
  - When `emulate_1541` is disabled or no 1541 ROM is loaded, the D64-backed KERNAL LOAD trap remains active for compatibility, including `LOAD "$",8` and `LOAD "*",8`.
  - Regression coverage includes direct real-ROM `LOAD"*",8` from `GALENCIA.D64` and the runtime autorun path (`-a --disk 8=...`) with success checked by BASIC memory contents, not by host RUN logging.

## Media-level path (M0–M8, opt-in)

Plan: `md-files/c64m1541media.md`. Enabled with `[disk] media_1541=1` (and
`emulate_1541=1`). Configure dialog: **1541 media (GCR)**. Default remains
job-intercept only (media off).

| Piece | Status | Location |
|---|---|---|
| Config flag / INI / UI | Done (M0) | `c64_config.media_1541`, `[disk] media_1541`, checkbox |
| Disk VIA motor / stepper / WPS / density | Done (M1) | `c1541_media.c` via2 PB |
| GCR encode + standard track synthesis from D64 | Done (M2) | `c1541_gcr.c`, `c1541_media_build_from_d64` |
| Rotation, SYNC (PB7), Port A GCR byte, BYTE READY→SO | Done (M3) | `c1541_media_step` |
| Physical READ without intercept | Done (M3) | READ/SEARCH/VERIFY intercept skipped when tracks valid |
| WRITE hybrid | Done (M4) | D64 sector write + `c1541_media_poke_sector` GCR update |
| FORMT hybrid | Done (M5) | D64 track erase + `c1541_media_rebuild_track` |
| Port A flux write gate | Done (M4) | DDRA=$FF and PCR CB2 manual-low (`$C0`) |
| G64 mount (read-only) | Done (M6) | `tools/g64`, `c64_mount_g64`, media half-track attach |
| Loader matrix (v1) | Done (M7) | See `c64m1541media.md` § M7; G64 first-file LOAD automated |
| Idle / density harden | Done (M8) | Flux clock only when motor ready; VIA DS when programmed |
| Hardware UI | Done (M8) | Misc Hardware tab shows device 8/9 media mechanics |
| User docs | Done (M8) | manual + README cover media_1541 and G64 |
| Activity LEDs | Done | Shared green/red UI LEDs: machine event seqs + frontend host-time hold; cleared on reset. See `docs/status/FRONTEND_DEBUGGER.md`. |
| Media cache coherence | Done | `image_content_seq` / `built_from_seq` force GCR rebuild after offline D64 writes; `c64_set_config` syncs+invalidates when media mode toggles live. |
| Long post-sync gap align | Done | Seek + first `$030D` align to long (data) inter-sync gap; dual-BVC 2-byte skip so stage-3 equal-gap self-checks pass (Robocop). |
| Dual-bit / BA / T1 harden | Done | Freeze 1541 on BA only in custom ILOAD `$0A00–$0BFF` (not global — preserves GCR measure + raster `$D012`); badline release=3; idle VIA2 T1 suppress in custom drive RAM. |

Hardware naming (code + real 1541):

```text
via1 @ $1800  — serial IEC (already wired)
via2 @ $1C00  — disk controller (media target)
```

When media is active and tracks are valid:

- Stock DOS physical READ runs against rotating GCR tracks (verified:
  `LOAD"*"` from `GALENCIA.D64` in `test_c64_real_1541_load` media case).
- WRITE jobs still intercept to the D64 for persistence, then poke the matching
  GCR data block so subsequent media READs see the new data. FORMT erases the
  D64 track and rebuilds that track’s GCR. Port A flux write (write gate) is
  live for non-job paths; pure job-level Port A capture is not relied on yet.
- Unmount syncs dirty GCR then invalidates media tracks for that drive.
- Live config changes that enter/leave media mode (`emulate_1541` / `media_1541`)
  sync dirty GCR (when leaving) and invalidate track caches so the next media-on
  session rebuilds from the current D64. Sector/job/KERNAL writes bump
  `image_content_seq`; `ensure_tracks` rebuilds when that seq no longer matches
  `built_from_seq` (covers SAVEs done with media off, then LOAD"$" with media on).
- Media SAVE of a small PRG is covered in `test_c64_real_1541_load`.
- **G64** (M6, read-only): runtime `mount` auto-detects `GCR-1541` signature;
  tracks attach as GCR flux (including half-tracks when present). Requires
  `emulate_1541=1` + `media_1541=1` + 1541 ROM for loads (no D64 sector
  intercept). WRITE/FORMT return write-protect. File browser disk dialogs
  accept any extension (drop `.g64` / `--disk` work).
- **M7 matrix (v1+):** stock media D64 LOAD/SAVE and G64 first-file LOAD are
  automated. Robocop G64 multi-stage **load-to-game** is automated
  (`test_c64_robocop_g64`: gap-table self-check + no BRK `$8072`). Full
  playthrough is not claimed. See `c64m1541media.md` § M7.

## ROM

- 1541 ROM loaded from INI key `[roms] 1541` or auto-discovered as `1541.rom` in `"."`, `"rom"`, `"roms"` directories.
- ROM must be the 16 KB combined image (lower 8 KB = 325302-01, upper 8 KB = 901229-06AA).
- Absent ROM is non-fatal: falls back to KERNAL LOAD trap.

## Known limitations / deferred

- Media **write** fidelity (Port A GCR write stream) and real FORMT GCR layout:
  still hybrid job-intercept + GCR poke/rebuild (M4/M5).
- G64 host write-back: not implemented (G64 mounts read-only v1).
- Fast loaders / nonstandard GCR: matrix expanded for dual-bit + Robocop
  stage-3; not an exhaustive commercial catalogue.
- Cross-drive copy (`C`), block/memory commands (`B-*`, `M-*`): deferred.
- Devices beyond 8 and 9: not implemented.
- 1571 and other ROM variants: not validated.
- NTSC 1541 clock (should be ~1.023 MHz vs PAL ~985 kHz): PAL 1:1 lockstep only for now.
- Save-state does not capture full GCR track buffers.
