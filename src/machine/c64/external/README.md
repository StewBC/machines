# C64 leftover external/

Shared vendors (argparse/inih/logc/stb/tiny-regex/whereami) live at repo-root
`external/` after Stage 2.

This directory keeps:

- `C64_TrueType_v1.2.1-STYLE/` — font/assets, not a CMake target
- `cia-timing-corpus/` — gitignored; `tools/cia-timing-corpus/fetch.sh` clones here
